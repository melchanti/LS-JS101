/*
input = String
output = Array of substrings

Explicit requirements:
  return all the substrings in string that are
  palindrom (palindrom are words that can be writtend the same forward and backward)

  Palindromes are case sensitive (Mom is not a palindrome)

Implicit requirements:
  an empty string should return an empty array
  substrings could be substrings of other substrings
  */